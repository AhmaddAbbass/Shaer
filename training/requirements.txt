# --- Core HF / training stack ---
transformers>=4.44.0
datasets>=3.0.0
accelerate>=1.0.0
peft>=0.13.0
trl>=0.9.6
huggingface_hub>=0.25.0
safetensors>=0.4.2
sentencepiece>=0.2.0
tqdm>=4.66.0

# --- Quantization / inference ---
bitsandbytes>=0.43.0
vllm>=0.6.0

# ⚠️ torch:
# If your Runpod image ALREADY has a working CUDA+torch+vLLM stack,
# you can comment this line out to avoid fighting with wheels.
torch

# --- Rewards + utilities ---
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.11.0
joblib>=1.3.0
scikit-learn>=1.4.0

# --- BiLSTM meter classifier stack ---
tensorflow==2.20.0
tf-keras==2.20.1
