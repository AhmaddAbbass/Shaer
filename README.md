# Shaer
Phase 1: build a tagged TAPT-ready corpus from the `arbml/ashaar` dataset.
Artifacts are written to `data/` and a stats report to `data/stats.md`.

more



initialize a venv

activate it

set hf token in .env

pip install requirements

python -m ipykernel install --user --name shaer-venv --display-name "Shaer venv"

then work lil bro